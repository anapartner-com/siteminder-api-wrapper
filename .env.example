# Server Configuration
NODE_ENV=production
PORT=3001
HOST=0.0.0.0

# SiteMinder API Configuration
SITEMINDER_BASE_URL=https://casso.cx.anapartner.net
SITEMINDER_USERNAME=siteminder
SITEMINDER_PASSWORD=anaPassword01

# Authentication Token Configuration (no longer needed - tokens refresh on-demand)
# TOKEN_RETRY_ATTEMPTS=3
# TOKEN_RETRY_DELAY=1000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# API Configuration
API_PREFIX=/api/v1
REQUEST_TIMEOUT=30000

# CORS Configuration
CORS_ORIGIN=*
CORS_CREDENTIALS=true
